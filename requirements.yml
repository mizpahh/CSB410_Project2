name: sl-mnist-env
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - tensorflow==2.18.0
      - numpy
      - pandas
      - matplotlib
      - seaborn
      - scikit-learn
      - jupyter
